require('./bootstrap');

require('alpinejs');

window.$ = window.jQuery = require('jquery');
import $ from 'jquery';
window.$ = window.jQuery = $;

require('./utilities/alerts');
require('./utilities/slugify');